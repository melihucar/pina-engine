Run the Pina Engine test suite.

## Instructions

1. Check if `build-macos-debug/bin/pina-tests` exists
   - If not, run `./build-macos.sh` first to build the project
2. Execute `./build-macos-debug/bin/pina-tests`
3. Report:
   - Total tests run
   - Passed/failed count
   - Any failure details with file:line references
