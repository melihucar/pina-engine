Clean build artifacts from the Pina Engine project.

## Instructions

1. Remove the following directories if they exist:
   - `build-macos-debug/`
   - `build-macos-release/`
2. Report what was cleaned and disk space freed
